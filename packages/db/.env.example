# Local dev database: Docker Postgres (docker run -d --name oliver-postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=oliver -p 5432:5432 postgres:16-alpine)
# Swap for any other Postgres (incl. `bunx prisma dev`) by changing this URL.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oliver"
